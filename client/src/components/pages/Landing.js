import React from 'react';

const Landing = () => (
        <div style={{textAlign:'center'}}>
	        <h1>Make appointment with us</h1>
	        with the help of the chatbot
        </div>
)

export default Landing;